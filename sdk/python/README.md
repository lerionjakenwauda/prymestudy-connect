# PrymeStudy Connect — Python SDK

Official server-side Python client for PrymeStudy Connect.

The SDK is framework-neutral and can be used from Django, Flask, FastAPI, background workers and other Python services.

Credentials and private signing keys must remain on trusted server-side systems.
